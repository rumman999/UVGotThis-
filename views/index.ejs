<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UV Got This?</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>🌞 UV Got This?</h1>
            <p>Check the UV index and stay safe under the sun!</p>
        </header>
        
        <form action="/submit" method="POST" class="search-form">
            <label for="city">Enter your city:</label>
            <input
                type="text"
                id="city"
                name="city"
                placeholder="Type your city name..."
                required
                autocomplete="off"
            />
            <div id="suggestions"></div>
            <button type="submit" class="submit-button">Get UV Info</button>
        </form>

        <% if (search) { %> 
            <div class="result">
                <p>🌞 UV index is currently <%= UV.toFixed(2) %> in <%= city %>.</p>
                <% if (UV < 2) { %>
                    <p>No sunscreen needed! Enjoy the sunshine!</p>
                <% } else { %>
                    <p>Don't forget your sunscreen! Stay safe under the sun.</p>
                <% } %>
            </div>
        <% } %>
    </div>

    <script src="/js/frontend.js"></script>
</body>
</html>
